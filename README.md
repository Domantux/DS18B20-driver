# DS18B20-driver
Linux kernel character device driver for the DS18B20 1-Wire temperature sensor, targeting Raspberry Pi 2. Implements the 1-Wire protocol via GPIO using the kernel's gpiod API, with device tree overlay support for hardware description.
